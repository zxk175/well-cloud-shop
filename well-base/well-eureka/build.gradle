apply plugin: 'org.springframework.boot'

bootJar {
    setArchivesBaseName('well-eureka')
}

dependencies {
    annotationProcessor libraries.lombok

    implementation([
            libraries.boot_undertow,
            libraries.boot_security,

            libraries.cloud_eureka_server,
    ])
}