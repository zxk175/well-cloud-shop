apply plugin: 'org.springframework.boot'

bootJar {
    setArchivesBaseName('well-config')
}

dependencies {
    compile([
            libraries.boot_undertow,

            libraries.cloud_config_server,
            libraries.cloud_eureka_client,
    ])
}
