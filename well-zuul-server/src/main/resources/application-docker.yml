eureka:
  client:
    service-url:
      defaultZone: ${eureka.list}

logging:
  level:
    root: info
    org.mybatis: trace
    com.alibaba: trace
    com.baomidou: trace
    org.springframework.cloud: trace