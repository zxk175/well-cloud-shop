mysql:
  # https://www.jianshu.com/p/7f96e322cb79
  host: docker.for.mac.host.internal

spring:
  datasource:
    druid:
      username: well
      password: 123456
  redis:
    host: ${mysql.host}
    password: 123456

mybatis-plus:
  configuration:
    # 打印sql语句,调试用
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

eureka:
  client:
    service-url:
      defaultZone: http://root:root@well-eureka:8001/eureka/