dependencies {
    annotationProcessor libraries.lombok

    implementation([
            project(":well-common:well-common-base"),

            libraries.mysql,
            libraries.mybatis_plus_generate,
    ])
}